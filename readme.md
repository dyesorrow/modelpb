#### install
npm install -g .

#### use

cd your model dir, edit model.config

```json
{
    "inputs": {
        "name": "path",
        ...
    },
    "outfile": "path"
}
```
then do

```shell
modelpb
```